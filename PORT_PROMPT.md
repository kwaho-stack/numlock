# (복붙용 프롬프트) 특수키 잠금 유틸리티 — 키별 토글 버전

아래 내용을 그대로 다른 AI/개발자에게 보내세요.

---

## 만들어줘: Windows용 "특수키 잠금" 트레이 유틸리티 (키별 ON/OFF 토글)

NumLock / CapsLock / ScrollLock / Insert 등 특수키의 동작을 **각각 개별 토글**로 켜고 끌 수 있는
가벼운 Windows 백그라운드(트레이) 프로그램을 만들어줘. 각 기능은 체크박스처럼 독립적으로 on/off 되고,
설정은 저장되어 재부팅 후에도 유지되며, 토글은 **즉시(재부팅 없이) 반영**돼야 해.

### 토글 가능한 기능 (각각 독립적으로)
1. **NumLock 항상 ON** — 숫자패드가 항상 숫자를 입력하도록 강제
2. **CapsLock 항상 OFF** — 캡스락 키 비활성화
3. **ScrollLock 항상 OFF** — 스크롤락 키 비활성화
4. **Insert 비활성화** — 전용 Insert 키만 비활성화 (숫자패드 0은 영향 없어야 함)
5. (확장 가능하게 설계: 위 패턴으로 다른 키도 쉽게 추가)

### UI
- 시스템 트레이 아이콘 + 우클릭 메뉴(또는 작은 설정 창)에 기능별 체크 항목
- "Windows 시작 시 자동 실행" 토글
- 설정은 파일(JSON 등)이나 레지스트리에 저장

### 반드시 알아둘 핵심 구현 노하우 (실제로 검증된 내용)

**A. 기본 메커니즘은 "저단계 키보드 후크"(WH_KEYBOARD_LL)를 권장.**
- 토글을 즉시 반영하려면 후크 방식이 최적이야. (레지스트리 Scancode Map은 변경 때마다
  키보드 장치 재시작/재부팅이 필요해서 동적 토글에 부적합. 다만 "로그인 화면에서도 적용"이
  필요하면 Scancode Map을 보조 옵션으로 제공해도 됨.)
- 후크 프로세스는 **관리자 권한(상승된 권한)으로 실행**해야 권한 높은 앱에서도 키가 잡힘.
  자동 실행은 **작업 스케줄러에 "최고 권한 + 로그인 시 실행 + Interactive"**로 등록하는 게 안정적.
- 전역 저단계 후크는 **메시지 펌프(GetMessage 루프)**가 도는 스레드에 설치해야 동작함.
  후크 콜백 델리게이트/함수 포인터는 GC/수거되지 않도록 참조를 살려둘 것.

**A-1. ⚠️ "시간이 지나면 잠금이 풀리는" 문제 — 반드시 처리.**
- Windows는 후크 콜백이 한 번이라도 **`LowLevelHooksTimeout`(기본 300ms)**을 초과하면
  (예: 매니지드 런타임의 GC 멈춤, 콜백 내 무거운 작업) **그 후크를 조용히 제거**해 버림.
  그러면 그 시점부터 키가 다시 토글됨. 이게 "조금 있으면 다시 풀림"의 원인.
- 대책 ①(필수): **워치독** — 타이머로 주기적으로(예: 2초마다) 후크를 재설치.
  새 후크를 먼저 SetWindowsHookEx 하고, 성공하면 옛 핸들을 UnhookWindowsHookEx 해서
  **잠기지 않는 빈틈이 없게** 교체. (새 설치 실패 시 옛 후크 유지.)
- 대책 ②(권장): `HKCU\Control Panel\Desktop\LowLevelHooksTimeout`(DWORD, ms)를
  크게(예: 10000) 설정해 애초에 제거 가능성을 낮춤. (완전 적용은 재로그인 필요, 그 전엔 워치독이 커버.)
- 대책 ③: 콜백을 최대한 가볍게. 무거운 작업은 콜백 밖(타이머/별도 처리)으로.
- 가능하면 후크 콜백/주입 로직을 **네이티브(C/C++/Rust)**로 두면 GC 멈춤 자체가 없어 더 안정적.

**B. 잠금 키(CapsLock/NumLock/ScrollLock) 차단 — "서명(signature) 트릭" 필수.**
- 후크에서 해당 키 이벤트를 삼켜서(return 1) 토글을 막되, **우리가 직접 주입하는 입력은 통과**시켜야
  상태를 강제할 수 있음. 구분 방법: 우리가 `keybd_event`/`SendInput`으로 주입할 때 `dwExtraInfo`에
  고유 서명값(예: 0x4B4C4F4B)을 넣고, 후크에서 `dwExtraInfo`가 그 서명이 아니면 차단, 맞으면 통과.
- 단순히 "injected 플래그(LLKHF_INJECTED)"만으로 구분하면 **다른 소프트웨어(예: 로지텍 Options)가
  주입한 키까지 통과**시켜 버려서 토글이 새는 버그가 생김. 반드시 우리만의 서명으로 구분할 것.
- 관련 VK 코드: `VK_CAPITAL=0x14`, `VK_NUMLOCK=0x90`, `VK_SCROLL=0x91`, `VK_INSERT=0x2D`.

**C. NumLock은 "상태 강제"가 안 통하는 키보드가 많음 (예: Lofree 등 펌웨어 키보드).**
- 이런 키보드는 숫자패드가 숫자/방향키 중 무엇을 보낼지 **키보드 펌웨어가 자체적으로** 결정해서,
  Scancode Map이나 Windows NumLock 상태 강제로는 못 막음 (LED만 켜지고 실제로는 토글됨).
- **해결책**: NumLock 상태와 싸우지 말고, **숫자패드 키가 "방향키"로 들어오면 즉시 해당 "숫자"로
  변환**해서 출력해. 그러면 키보드가 뭘 하든 숫자패드는 항상 숫자가 찍힘.
- 구분 방법: 숫자패드 키는 **스캔코드가 숫자패드 범위이고 확장(extended) 플래그가 없음**.
  진짜 방향키/네비 클러스터(Home/End/방향키 등)는 **확장 플래그가 설정됨(E0 prefix)**. 이걸로 구분.
- 후크에서: 이벤트가 (확장 아님) && (스캔코드가 숫자패드) && (vkCode가 이미 숫자패드 키가 아님)이면,
  원래 이벤트는 삼키고 대응하는 숫자패드 VK를 서명을 달아 주입.
- 숫자패드 스캔코드 → 숫자 VK 매핑표:

  | scancode | 의미 | 주입할 VK |
  |---|---|---|
  | 0x47 | 7 | VK_NUMPAD7 0x67 |
  | 0x48 | 8 | VK_NUMPAD8 0x68 |
  | 0x49 | 9 | VK_NUMPAD9 0x69 |
  | 0x4B | 4 | VK_NUMPAD4 0x64 |
  | 0x4C | 5 | VK_NUMPAD5 0x65 |
  | 0x4D | 6 | VK_NUMPAD6 0x66 |
  | 0x4F | 1 | VK_NUMPAD1 0x61 |
  | 0x50 | 2 | VK_NUMPAD2 0x62 |
  | 0x51 | 3 | VK_NUMPAD3 0x63 |
  | 0x52 | 0 | VK_NUMPAD0 0x60 |
  | 0x53 | . | VK_DECIMAL 0x6E |

  (주입한 VK_NUMPADx가 숫자로 찍히려면 Windows NumLock 상태가 ON이어야 하므로, NumLock 토글이
  켜진 동안에는 NumLock 상태를 ON으로 유지하는 보조 로직도 둘 것. 0.25초 주기 타이머로 꺼지면
  다시 켜는 식.)

**D. Insert 비활성화 — 전용 Insert 키만.**
- 전용 Insert 키는 **확장(extended) 플래그가 설정된** `VK_INSERT`임. 이것만 차단.
- 숫자패드 0(스캔코드 0x52, 확장 아님)은 위 C의 변환으로 "0"이 되어야 하므로 건드리지 말 것.

**E. (선택) 레지스트리 Scancode Map 보조 모드.**
- "로그인 화면에서도 적용"이 필요하면, CapsLock(0x3A)/ScrollLock(0x46)/Insert(확장 0xE052) 등을
  `HKLM\SYSTEM\CurrentControlSet\Control\Keyboard Layout`의 `Scancode Map`(REG_BINARY)으로
  드라이버 레벨 비활성화 가능. 형식: [버전4B][플래그4B][매핑개수4B][매핑들(각 4B: 새코드2B=0000, 원래코드2B)][종료자4B].
  변경을 재부팅 없이 반영하려면 키보드 장치를 Disable/Enable(PnP)로 재시작. (NumLock은 이 방식이
  안 통하는 키보드가 있으니 C의 후크 방식을 쓸 것.)
- 부팅 시 NumLock ON: 모든 `HKEY_USERS\<SID>\Control Panel\Keyboard`의
  `InitialKeyboardIndicators`를 `2`로 설정.

### 구현/배포 주의사항
- 기술 스택은 자유(C#/.NET WinForms·WPF, C++/Win32, Rust, AutoHotkey 등). 가볍고 단일 실행에 가깝게.
- 전역 저단계 후크는 **메시지 펌프(message loop)**가 돌아가는 스레드에 설치해야 함.
- 후크 콜백 델리게이트는 GC에 수거되지 않도록 참조를 유지할 것.
- (PowerShell로 만든다면) 스크립트는 BOM 인코딩 문제로 깨질 수 있으니 ASCII로 작성하거나 UTF-8 BOM 사용.
- 각 토글을 끄면 해당 차단/변환 로직이 즉시 비활성화돼야 함(런타임에 플래그만 바꿔 후크 동작을 분기).

### 산출물
- 빌드된 실행 파일 + 소스
- 설치/자동실행 등록 방법
- 각 토글이 즉시 동작하는지 확인하는 간단한 테스트 안내
